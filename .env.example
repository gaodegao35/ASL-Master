# No environment variables or API keys are required.
# ASL Master runs entirely in the browser — hand detection is done locally
# with MediaPipe (bundled under public/mediapipe), with no server or LLM calls.
